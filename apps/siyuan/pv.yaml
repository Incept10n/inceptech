apiVersion: v1
kind: PersistentVolume
metadata:
  name: siyuan-local-pv
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain   # <-- critical
  storageClassName: local-storage         # custom class
  local:
    path: /mnt/siyuan-data                # path on your node
