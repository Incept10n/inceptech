apiVersion: apps/v1
kind: Deployment
metadata:
  name: siyuan
  namespace: siyuan
  labels:
    app: siyuan
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: siyuan
  template:
    metadata:
      labels:
        app: siyuan
      annotations:
        secret/checksum: "managed-by-eso"
    spec:
      containers:
        - name: siyuan
          image: b3log/siyuan:latest
          imagePullPolicy: Always
          command: ["/bin/sh", "-c"]
          args:
            - |
              exec /opt/siyuan/entrypoint.sh serve \
                --workspace=/siyuan/workspace/ \
                --accessAuthCode="${SIYUAN_ACCESS_AUTH_CODE}"
          ports:
            - name: http
              containerPort: 6806
              protocol: TCP
          env:
            - name: TZ
              value: "Europe/Moscow"
            - name: SIYUAN_ACCESS_AUTH_CODE
              valueFrom:
                secretKeyRef:
                  name: siyuan-secrets
                  key: accessAuthCode
          volumeMounts:
            - name: workspace
              mountPath: /siyuan/workspace
          resources:
            requests:
              cpu: "50m"
              memory: "256Mi"
            limits:
              cpu: "50m"
              memory: "256Mi"
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: siyuan-workspace
      restartPolicy: Always
