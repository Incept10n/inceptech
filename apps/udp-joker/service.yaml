apiVersion: v1
kind: Service
metadata:
  name: udp-joker
  namespace: udp-joker
spec:
  type: ClusterIP
  selector:
    app: udp-joker
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
