apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: watchly-server-tls
  namespace: nginx-load-balancer
spec:
  secretName: watchly-server-tls
  issuerRef:
    name: regru-dns
    kind: ClusterIssuer
  dnsNames:
    - watchly.inceptech.ru
